version: '3.8'

services:
  fastapi-server:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    env_file:
      - .env
    environment:
      - PORT=${PORT:-8000}
    volumes:
      - .:/app
